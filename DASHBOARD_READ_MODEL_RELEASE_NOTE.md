# Dashboard Read Model — Release Note

Added Dashboard Read Model 1.0, source lineage, fixed timeframe/module projections, fail-closed display states and focused tests. Existing Dashboard WSGI/HTML/API behavior is unchanged. Run `python -m pytest tests/test_dashboard_read_model.py -q` then `python -m pytest -q`.
