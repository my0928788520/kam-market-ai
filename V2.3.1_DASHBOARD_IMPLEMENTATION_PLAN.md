# V2.3.1 正式 Dashboard 最小實作計畫

> 本文件只規劃根主線 `src/kam_market_ai` 的正式首頁。`KAM_V1.6_fubon_bridge/` 維持並列舊版，不作為實作來源、執行依賴或修改目標。

## 目標與邊界

V2.3.1 建立一個可本機啟動的正式 Dashboard：使用既有 Python 技術棧與標準庫，不導入 React/Vue、外部 Web framework、下單、AI 摘要、3/5/10 日區間、動畫或背景高頻輪詢。

首頁固定呈現以下七個區塊：

1. 市場摘要
2. 市場方向
3. 市場控制權
4. 倒 U 市場生命週期
5. 週／日／60／15
6. 目前部位
7. 下一步

本版僅將「目前部位」接通真實持倉 read model。其他六個區塊以明確的靜態／尚未接通資料呈現，不假裝為即時市場結論。

## 1. 建議目錄結構

```text
src/kam_market_ai/
├─ dashboard/
│  ├─ __init__.py
│  ├─ models.py                 # Dashboard 與 Position read model dataclass
│  ├─ position_reader.py        # 只讀取 position snapshot，不呼叫 SDK
│  ├─ payload.py                # Dashboard JSON payload builder
│  ├─ app.py                    # 最小 WSGI application：HTML 與 /api/dashboard
│  ├─ templates/
│  │  └─ index.html             # 固定首頁布局，不使用前端框架
│  └─ static/
│     └─ dashboard.css          # 純 CSS；不得加入動畫
├─ positions/
│  └─ ...                       # 保持目前 parser／matching 實作
└─ ...
tools/
└─ run_dashboard.py             # wsgiref 本機啟動器；不登入、不查詢 SDK
tests/
├─ test_dashboard_payload.py
├─ test_dashboard_position_reader.py
└─ fixtures/dashboard/
   ├─ position_short_one.json
   ├─ position_long_one.json
   ├─ position_empty.json
   ├─ position_query_failed.json
   └─ position_timeout.json
debug/position/
└─ dashboard_position_snapshot.json  # 本機、gitignore 的唯一 Dashboard 持倉輸入
```

## 2. 預計新增與修改檔案

| 動作 | 檔案 | 責任 |
| --- | --- | --- |
| 新增 | `src/kam_market_ai/dashboard/models.py` | `PositionDashboardState`、`DashboardSnapshot` 與固定區塊 model。 |
| 新增 | `src/kam_market_ai/dashboard/position_reader.py` | 讀取並驗證 `debug/position/dashboard_position_snapshot.json`；檔案缺失／格式錯誤視為同步異常，不可視為空倉。 |
| 新增 | `src/kam_market_ai/dashboard/payload.py` | 建立 schema versioned JSON；組裝靜態市場區塊與真實 position 區塊。 |
| 新增 | `src/kam_market_ai/dashboard/app.py` | Python 標準庫 WSGI 路由：`GET /`、`GET /api/dashboard`、靜態 CSS。只讀本機 snapshot。 |
| 新增 | `src/kam_market_ai/dashboard/templates/index.html` | 固定七區塊首頁；以最少原生 JavaScript 載入 `/api/dashboard`，不使用框架或動畫。 |
| 新增 | `src/kam_market_ai/dashboard/static/dashboard.css` | 原生 CSS 格線與狀態色；不改設計為動畫式介面。 |
| 新增 | `tools/run_dashboard.py` | `wsgiref.simple_server` 啟動器；只提供本機頁面，不登入富邦。 |
| 修改 | `tools/probe_fubon_position.py` | 成功／空／timeout／exception 時，原子寫入唯一 `dashboard_position_snapshot.json`；僅寫去識別化 position payload 和同步狀態。不得加入輪詢。 |
| 修改 | `.gitignore` | 已忽略 `debug/position/`；僅確認 snapshot 沿用此保護規則。 |
| 新增 | `tests/test_dashboard_position_reader.py` | snapshot 成功、空、失敗、timeout、缺檔與無效 JSON 規則。 |
| 新增 | `tests/test_dashboard_payload.py` | JSON schema、部位文案、價格／損益格式及靜態生命週期區塊。 |
| 新增 | `tests/test_dashboard_app.py` | WSGI HTML/JSON routes 與首頁固定區塊存在。 |
| 新增 | `tests/fixtures/dashboard/*.json` | 去識別化持倉狀態 fixtures。 |

本階段不修改 `PositionRawAdapter`、`PositionNormalizer`、`match_mtx_positions`、Rule Engine、`execution/`、authorization、帳密／憑證、或任何 V1.x 檔案。

## 3. Dashboard payload schema

Dashboard API 回應版本為 `V2.3.1`。position 是唯一真實接通區塊：

```json
{
  "schema_version": "V2.3.1",
  "generated_at": "2026-07-29T00:00:00Z",
  "market_summary": {"status": "STATIC", "message": "市場資料尚未接通"},
  "market_direction": {"status": "STATIC", "message": "尚未接通"},
  "market_control": {"status": "STATIC", "message": "尚未接通"},
  "market_lifecycle": {"status": "STATIC", "shape": "INVERTED_U", "message": "倒 U 生命週期區塊保留，尚未接通市場資料"},
  "timeframes": {"status": "STATIC", "periods": ["1W", "1D", "60m", "15m"]},
  "position": {
    "query_success": true,
    "matched_rows": 1,
    "product_code": "MTX",
    "symbol_raw": "FITM",
    "expiry": "202608",
    "side": "SHORT",
    "quantity": 1,
    "average_price": "40461.0",
    "market_price": "40314.0000",
    "unrealized_pnl": "1470.0",
    "updated_at": "2026-07-29T00:00:00Z",
    "status": "SINGLE_MTX_POSITION",
    "display_state": "POSITION"
  },
  "next_step": {"status": "STATIC", "message": "尚未接通決策引擎"}
}
```

`average_price`、`market_price`、`unrealized_pnl` 保留 Decimal string，不以 float 再運算。

### Position 狀態規則

| 輸入狀態 | `display_state` | 首頁內容 |
| --- | --- | --- |
| `query_success=true`、`matched_rows=1`、status 可用 | `POSITION` | 「我的部位／微台／多單或空單 ×N／均價／未實現／同步時間」。 |
| `query_success=true`、`matched_rows=0` | `EMPTY` | 「目前無部位」。 |
| query failed、timeout、snapshot 缺失、stale、無效 schema、parser failure | `SYNC_ERROR` | 「持倉同步異常」與安全 reason code；絕不可顯示「目前無部位」。 |

`updated_at` 由已完成的 position capture 寫入；Dashboard 不自行呼叫富邦 SDK，因此不會形成隱藏查詢或背景同步。

## 4. 首頁最小顯示行為

成功且匹配一口時，「目前部位」卡顯示：

```text
我的部位
微台
空單 ×1
均價：40,461.0
未實現：+1,470.0
同步：2026-07-29 08:00:00 UTC
```

多單同理，顯示「多單 ×1」。價格、損益使用 Decimal string 的千分位顯示；無值顯示 `—`，不顯示假設價格。

## 5. 倒 U 市場生命週期區塊

保留固定區塊與 `INVERTED_U` 結構，不新增動畫、不宣稱即時狀態。初版使用靜態 SVG/CSS 線條與明確文字：

```text
倒 U 市場生命週期
市場資料尚未接通（靜態占位）
```

未來市場結構資料接入時，僅替換 payload 的 lifecycle state／marker；HTML 區塊、CSS layout 與 API schema 不需重設計。

## 6. 啟動方式

預計提供：

```powershell
$env:PYTHONPATH = "src"
& "C:\Users\my092\AppData\Local\Programs\Python\Python312\python.exe" tools\run_dashboard.py --host 127.0.0.1 --port 8765
```

瀏覽器開啟 `http://127.0.0.1:8765/`；JSON 可於 `http://127.0.0.1:8765/api/dashboard` 檢查。啟動器不執行 SDK login，也不產生 position query。

## 7. 測試方式

```powershell
& "C:\Users\my092\AppData\Local\Programs\Python\Python312\python.exe" -m pytest -q
```

最低測試案例：

- 真實 schema 對應的 MTX/FITM 空單一口：首頁 payload 顯示微台、空單 ×1、均價、未實現損益。
- MTX 多單一口：顯示多單 ×1。
- 成功但零筆 matched rows：只顯示「目前無部位」。
- query exception：`SYNC_ERROR`，不得含「目前無部位」。
- timeout：`SYNC_ERROR`，不得含「目前無部位」。
- 缺 snapshot／過期 snapshot／不合法 JSON：`SYNC_ERROR`。
- Decimal 字串格式、JSON schema 與七固定區塊。
- 所有既有測試持續通過。

## 8. 最小交付順序

1. 定義 position snapshot schema 與 fixtures，先完成 `position_reader` 的 fail-closed 測試。
2. 讓一次性 probe/capture 成功、空、timeout、exception 都原子寫入 snapshot 狀態；不加排程。
3. 建立 payload builder，先只以 fixture 接通 position，其餘區塊固定 static。
4. 建立 WSGI JSON endpoint 與 HTML/CSS 七區塊頁面。
5. 加入本機啟動器與端對端 WSGI 測試。
6. 執行完整測試集，確認 Dashboard 未觸發 SDK、委託或交易行為。

## 9. 未來即時點數接入方向（本版不實作）

未來可由現有 `market_data/FubonNeoMarketDataAdapter` 將已授權的市場資料正規化為 quote read model，再由 payload builder 讀取。位置是：

```text
FubonNeoMarketDataAdapter → Quote read model → dashboard/payload.py → /api/dashboard
```

Dashboard 不直接持有 SDK client、不直接訂閱 WebSocket，也不從 position payload 推導即時點數。接入時仍需遵守現有 market-data-only boundary、資料新鮮度與 session gate；V2.3.1 Dashboard 本版不實作此能力。

