# Replay Read-Only UI

Version 1.0 renders the Presenter context with fixed sections and fixed IDs. It has no JavaScript. The CSS is standalone, responsive from 320px through desktop, keyboard-visible, reduced-motion aware, and uses text alongside state color. Values are escaped plain text and unavailable values remain unavailable.
