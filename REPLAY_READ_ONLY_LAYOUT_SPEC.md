# Replay Read-Only Layout

The top of the page prioritizes banner, identity, hero, progress, decision summary, and comparison. Cards follow in fixed order. Progress reports position only and does not control replay. Invalid states retain the same landmarks and placeholders. Future navigation or playback requires a separate contract.
