# Sprint 8A Public Deployment and Blog Embed Contract

This contract defines explicit HTTPS-only frame ancestors, read-only deployment mode, embed route metadata, and static health/readiness payloads. It contains no server routing, HTML, dashboard, account, market-data read, credential, WebSocket, or trading capability. Invalid ancestors fail closed; wildcard, localhost, paths, queries, and non-HTTPS origins are rejected.
