# Dashboard Fixture Contract

Fixtures wrap serialized payloads with versioned metadata and explicit assertions. They are deterministic, portable JSON envelopes and contain no credentials, account identifiers, tokens, or execution instructions. Metadata and payload serialization versions must match; malformed version or timeframe coverage fails validation.
