# Dashboard Serialization — Release Note

Added Serialization 1.0, fixture envelope helpers, canonical JSON helper and round-trip coverage. Existing Read Model, WSGI Dashboard, HTML/CSS and API are unchanged. Run `python -m pytest tests/test_dashboard_serialization.py -q` then `python -m pytest -q`.
