# V2.3.1 富邦持倉後端狀態

> 狀態日期：2026-07-29。本文只記錄已完成、唯讀的持倉同步後端成果；不代表已完成 Dashboard／首頁整合。

## 1. 富邦正式持倉查詢 method

正式的完整未平倉持倉查詢為：

```python
FubonSDK.futopt_accounting.query_hybrid_position(futopt_account)
```

其中 `futopt_account` 的實際 Python 型別為 `builtins.Account`，且 `account_type == "futopt"`。

已驗證的單腿未平倉查詢為：

```python
FubonSDK.futopt_accounting.query_single_position(futopt_account)
```

本次真實 row capture 使用 `query_single_position`；它回傳 `CustomReturnType`，其 `data` 為 `builtins.list`，並已成功取得一筆 Position row。所有查詢均為唯讀，不含行情、委託、成交或交易操作。

## 2. 真實 Position row schema

真實持倉 row 型別：`builtins.Position`。

公開欄位如下：

```text
account, branch_no, buy_sell, call_put, clearing_margin, currency, date,
expiry_date, initial_margin, maintenance_margin, market_price, order_no,
order_type, orig_lots, position_kind, premium, price, profit_or_loss,
strike_price, symbol, tradable_lot
```

去識別化 capture 已移除／遮罩：`account`、`branch_no`、`order_no`。帳密、憑證、token、姓名、身分證及其他客戶識別資料不會寫入 debug 檔。

已保留的交易相關欄位包括：`symbol`、`expiry_date`、`buy_sell`、`orig_lots`、`tradable_lot`、`price`、`market_price`、`profit_or_loss`、`currency` 與非個資保證金欄位。

## 3. 商品與欄位映射

| 富邦真實欄位／值 | 正規化結果 |
| --- | --- |
| `symbol = FITM` | `product_code = MTX` |
| `expiry_date = 202608` | `contract_month = "202608"` |
| `buy_sell = BSAction.Sell` | `side = SHORT` |
| `orig_lots = 1` | `quantity = 1` |
| `price` | `average_price` |
| `market_price` | `current_price`（僅使用持倉回傳值，不新增行情查詢） |
| `profit_or_loss` | `unrealized_pnl` |

`FITM` 已由既有 alias 策略正規化為微型臺指 `MTX`；原始代碼仍保留於 `symbol_raw`。

## 4. 已驗證的解析結果

- 商品：`MTX`（原始 symbol：`FITM`）
- 到期月份：`202608`
- 方向：`SHORT`
- 口數：`1`
- 均價：成功解析
- 現價：成功解析自 `market_price`
- 未實現損益：成功解析自 `profit_or_loss`
- `matched_rows = 1`
- match status：`SINGLE_MTX_POSITION`

相關去識別化輸出位於 `debug/position/`：

- `fubon_single_position_raw.json`
- `raw_position.json`
- `normalized_position.json`
- `matched_position.json`
- `parser.log`

## 5. 測試狀態

目前完整測試結果：**90 passed**。

測試已涵蓋 MTX 多／空單一口、FITM/TMF/MTX aliases、真實 FITM row schema fixture、缺欄位、字串數字、負數口數、方向衝突、空持倉、未知商品、debug 輸出及 probe timeout／遮罩行為。

## 6. Dashboard 尚未整合的原因

目前 Repository 的根主線 `src/kam_market_ai` 沒有正式 Dashboard JSON 產生器、首頁前端或倒 U 市場生命週期圖。

`KAM_V1.6_fubon_bridge/` 是並列舊版，不是 V2.3.1 正式 Dashboard 主線；本階段**不修改、不刪除、不整合**該目錄的任何檔案。

因此持倉後端已完成可驗證的資料事實層與 parser，但尚無正確的現行首頁目標可安全接入。

## 7. 後續 Dashboard 整合 payload 契約

當正式 Dashboard 主線確立後，持倉 read model 應以如下 JSON schema 接入：

```json
{
  "position": {
    "query_success": true,
    "matched_rows": 1,
    "product_code": "MTX",
    "symbol_raw": "FITM",
    "expiry": "202608",
    "side": "SHORT",
    "quantity": 1,
    "average_price": "<decimal-string-or-null>",
    "market_price": "<decimal-string-or-null>",
    "unrealized_pnl": "<decimal-string-or-null>",
    "updated_at": "<ISO-8601 timestamp>",
    "status": "SINGLE_MTX_POSITION"
  }
}
```

狀態規則：

- `query_success=true` 且 `matched_rows=1`：顯示已匹配部位。
- `query_success=true` 且 `matched_rows=0`：才可顯示「目前無部位」。
- `query_success=false`、timeout、parser failure 或 stale data：顯示「持倉同步異常」，不得以空持倉替代。

此契約僅描述讀取／顯示資料；不包含下單、改單、刪單、行情訂閱、背景高頻輪詢或帳密設定。

