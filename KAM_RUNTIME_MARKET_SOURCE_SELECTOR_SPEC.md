# Sprint 7 Runtime Source Selector

The runtime defaults to `offline-demo`. `fake-live` is explicit, local, deterministic, and read-only. `fugle-live` is reserved, reports `真實行情來源尚未啟用`, and has no available products or fallback. The selector and provider have no credentials, account, order, position, balance, or trading capability.
