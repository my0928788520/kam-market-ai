# Replay Dashboard Serialization

Version 1.0 serializes only typed Replay Dashboard models and comparison contracts. JSON is canonical UTF-8 data: sorted keys, ISO datetimes, string Decimals, enum values, and arrays for tuples. It contains no HTML, callable representation, local path, exception stack, or secret.
