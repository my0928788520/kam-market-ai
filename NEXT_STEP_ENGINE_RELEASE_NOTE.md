# Next Step Engine Foundation — Release Note

- Added Next Step Engine version 1.0 with typed safe observation/verification actions, operational states, priorities, reasons, triggers and timeframe outputs.
- Added source-integrity hard blocks and deterministic priority handling.
- Added offline tests for observation, wait-for-close, invalid source and configuration validation.

Run `python -m pytest tests/test_next_step_engine.py -q`, then `python -m pytest -q`. The feature has no order capability and all mappings remain **PROVISIONAL**.
